<UserControl x:Class="Rofl.UI.Main.Controls.ReplayDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Rofl.UI.Main.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="610" d:DesignWidth="560">

    <StackPanel Orientation="Vertical"
                Background="White"
                Margin="5">
        <StackPanel Orientation="Vertical">
            <TextBox FontSize="25"
                     Background="Transparent"
                     BorderThickness="0"
                     IsReadOnly="True"
                     Text="{Binding Path=PreviewModel.Name, FallbackValue=Name, Mode=OneWay}"/>
            <StackPanel Orientation="Horizontal">
                <TextBox Background="Transparent"
                         BorderThickness="0"
                         IsReadOnly="True"
                         Text="{Binding Path=PreviewModel.MatchId, FallbackValue=Match id, Mode=OneWay}"/>
                
                <TextBlock Text="-" Padding="5 0 5 0"/>
                <TextBox Background="Transparent"
                         BorderThickness="0"
                         IsReadOnly="True"
                         Text="{Binding Path=PreviewModel.MapName, FallbackValue=Map name, Mode=OneWay}"/>
                
                <TextBlock Text="-" Padding="5 0 5 0"/>
                <TextBox Background="Transparent"
                         BorderThickness="0"
                         IsReadOnly="True"
                         Text="{Binding Path=PreviewModel.GameLengthString, FallbackValue=Game duration, Mode=OneWay}"/>
                

                <TextBlock Text="-" Padding="5 0 5 0"/>
                <TextBox Background="Transparent"
                         BorderThickness="0"
                         IsReadOnly="True"
                         Text="{Binding Path=PreviewModel.GameVersion, FallbackValue=Game version, Mode=OneWay}"/>
            </StackPanel>
        </StackPanel>
    </StackPanel>
    
</UserControl>
